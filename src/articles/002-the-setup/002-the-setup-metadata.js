const rootURL = 'https://robbycowell.com';
const articlesURL = 'dispatches';

const slug = '001-the-setup'

const metadata = {
  publishDate: new Date(Date.now()),
  slug,
  permalink: `${rootURL}/${articlesURL}/${slug}`
};

export default metadata;
